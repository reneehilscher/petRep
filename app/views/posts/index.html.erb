<h1>All Posts</h1>
 
<div class="posts">

  <% @posts.each do |post| %>
    <div class="post-single">
      <h2><%= post.title %></h2>
      <p><%= post.description %></p>
    </div>
    <%= image_tag post.image_url.to_s %>
  <% end %>
</div>

<%= link_to 'Back', root_path %>